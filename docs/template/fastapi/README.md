# FastAPI Template Setup

## Consuming Template

The following is to be executed within the new FastAPI repository

```shell
git remote add template git@github.com-kmjbyrne:kmjbyrne/python-template-fastapi.git
git fetch template
git merge template/main
```
