# Copy to .env and edit. Every setting in app/config.py can be overridden here;
# these are the ones that usually need it. Real environment variables win over
# anything set in a file.

PROJECT_NAME="CHANGEME"
ENVIRONMENT="development"  # development, test, staging, production

# Logging goes to stdout. Set LOG_JSON=true for one JSON object per line.
LOG_LEVEL="INFO"
LOG_JSON="false"

# JSON list of origins allowed by the CORS middleware. Credentials are enabled,
# and browsers reject credentialed responses with a wildcard origin, so list
# origins explicitly rather than using "*".
CORS_ORIGINS='["http://localhost:3000"]'

# persistence:begin
DATABASE_URL="sqlite:///instance/database.db"
# persistence:end
